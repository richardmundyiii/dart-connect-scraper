const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const standingsSchema = new Schema({
  week: {
    type: String,
    required: true,
  },
});

module.exports = mongoose.model("Standing", standingsSchema);
